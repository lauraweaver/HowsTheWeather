var app = new AppView({
    //Bootstrapping
// this is in lieu of putting it in the initialize functon in the AppView
//you can reference models and collections by passing them in 
  collection: new Weather()
});
